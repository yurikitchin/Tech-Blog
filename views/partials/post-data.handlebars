    <div>
        <h1 class="postTitle" id={{id}}>{{title}}</h1>
        <p>created by {{user.userName}} on {{datePosted}} this is format date {{format_date}}</p>
    </div>
    <article>
        {{blog}}
    </article>
  